import {NextResponse} from 'next/server';import {cookies} from 'next/headers';import {db} from '../../../../../lib/db';import {verifyAdminToken} from '../../../../../lib/qnaAuth';
export const runtime='nodejs';export const dynamic='force-dynamic';
async function admin(){const store=await cookies();return verifyAdminToken(store.get('olbarun_admin')?.value);}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){if(!await admin())return NextResponse.json({ok:false,message:'관리자 로그인이 필요합니다.'},{status:401});const {id}=await params;if(!/^\d+$/.test(id))return NextResponse.json({ok:false,message:'잘못된 요청입니다.'},{status:400});try{const b=await request.json();const title=String(b.title||'').trim().slice(0,160),content=String(b.content||'').trim().slice(0,10000),createdAt=String(b.createdAt||'').trim(),viewCount=Number(b.viewCount);if(title.length<2||content.length<2||!/^\d{4}-\d{2}-\d{2}$/.test(createdAt)||!Number.isInteger(viewCount)||viewCount<0)return NextResponse.json({ok:false,message:'제목과 내용을 확인해 주세요.'},{status:400});const sql=db();const rows=await sql`UPDATE notices SET title=${title},content=${content},created_at=make_timestamptz(split_part(${createdAt},'-',1)::int,split_part(${createdAt},'-',2)::int,split_part(${createdAt},'-',3)::int,12,0,0,'Asia/Seoul'),view_count=${viewCount} WHERE id=${Number(id)} RETURNING id`;if(!rows[0])return NextResponse.json({ok:false,message:'공지사항을 찾을 수 없습니다.'},{status:404});return NextResponse.json({ok:true});}catch(e){console.error(e);return NextResponse.json({ok:false,message:'수정 중 오류가 발생했습니다.'},{status:500});}}
export async function DELETE(_request:Request,{params}:{params:Promise<{id:string}>}){if(!await admin())return NextResponse.json({ok:false,message:'관리자 로그인이 필요합니다.'},{status:401});const {id}=await params;if(!/^\d+$/.test(id))return NextResponse.json({ok:false,message:'잘못된 요청입니다.'},{status:400});try{const sql=db();const rows=await sql`DELETE FROM notices WHERE id=${Number(id)} RETURNING id`;if(!rows[0])return NextResponse.json({ok:false,message:'공지사항을 찾을 수 없습니다.'},{status:404});return NextResponse.json({ok:true});}catch(e){console.error(e);return NextResponse.json({ok:false,message:'삭제 중 오류가 발생했습니다.'},{status:500});}}